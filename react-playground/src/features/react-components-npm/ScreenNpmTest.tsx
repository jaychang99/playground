import { Button } from '@jaychang99/react-components';

const ScreenNpmTest = () => {
  return (
    <Button
      onClick={() => {
        alert('버튼 클릭');
      }}
    >
      버튼
    </Button>
  );
};

export default ScreenNpmTest;
